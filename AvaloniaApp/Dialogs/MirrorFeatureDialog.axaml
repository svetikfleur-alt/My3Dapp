<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="My3DApp.AvaloniaApp.Dialogs.MirrorFeatureDialog"
        Width="420"
        Height="340"
        MinWidth="400"
        MinHeight="320"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Classes="FeatureDialogWindow"
        Title="Mirror">
  <Border Classes="FeatureDialogPanel" Margin="0">
    <Grid RowDefinitions="Auto,Auto,*,Auto" Margin="18">
      <Grid ColumnDefinitions="*,Auto">
        <StackPanel Spacing="4">
          <TextBlock Classes="FeatureDialogTitle"
                     Text="Mirror" />
          <TextBlock Classes="FeatureDialogSubtitle"
                     TextWrapping="Wrap"
                     Text="Reflect the selected body across a symmetry plane." />
        </StackPanel>
        <Border Grid.Column="1"
                Background="{DynamicResource Studio.PanelInsetBrush}"
                BorderBrush="{DynamicResource Studio.BorderBrush}"
                BorderThickness="1"
                Padding="9,5"
                VerticalAlignment="Top">
          <TextBlock Text="FEATURE"
                     FontSize="10"
                     FontWeight="SemiBold" />
        </Border>
      </Grid>

      <StackPanel Grid.Row="2"
                  Margin="0,16,0,16"
                  Spacing="12">
        <Border Background="{DynamicResource Studio.PanelInsetBrush}"
                BorderBrush="{DynamicResource Studio.BorderBrush}"
                BorderThickness="1"
                Padding="12">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="110,*">
            <TextBlock Text="Mirror plane"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <ComboBox x:Name="AxisComboBox"
                      Grid.Column="1"
                      Width="180"
                      HorizontalAlignment="Left"
                      SelectedIndex="0">
              <ComboBoxItem Content="Right (YZ) plane" Tag="x" />
              <ComboBoxItem Content="Front (XZ) plane" Tag="y" />
              <ComboBoxItem Content="Top (XY) plane" Tag="z" />
            </ComboBox>
            <TextBlock Grid.Row="1"
                       Grid.ColumnSpan="2"
                       Margin="0,10,0,0"
                       Foreground="{DynamicResource Studio.TextMutedBrush}"
                       TextWrapping="Wrap"
                       Text="The selected body is reflected across the chosen plane through the world origin." />
          </Grid>
        </Border>
      </StackPanel>

      <StackPanel Grid.Row="3"
                  Orientation="Horizontal"
                  HorizontalAlignment="Right"
                  Spacing="8">
        <Button Content="Cancel"
                Classes="FeatureDialogButtonSecondary"
                Click="OnCancelClick" />
        <Button Content="OK"
                Classes="FeatureDialogButtonPrimary"
                Click="OnConfirmClick" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
