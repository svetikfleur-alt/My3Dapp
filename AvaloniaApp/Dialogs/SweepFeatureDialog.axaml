<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="My3DApp.AvaloniaApp.Dialogs.SweepFeatureDialog"
        Width="460"
        Height="430"
        MinWidth="430"
        MinHeight="410"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Classes="FeatureDialogWindow"
        Title="Sweep">
  <Border Classes="FeatureDialogPanel" Margin="0">
    <Grid RowDefinitions="Auto,Auto,*,Auto" Margin="18">
      <Grid ColumnDefinitions="*,Auto">
        <StackPanel Spacing="4">
          <TextBlock Classes="FeatureDialogTitle"
                     Text="Sweep" />
          <TextBlock Classes="FeatureDialogSubtitle"
                     TextWrapping="Wrap"
                     Text="Create a solid by sweeping a closed sketch profile along the sketch plane normal." />
        </StackPanel>
        <Border Grid.Column="1"
                Background="{DynamicResource Studio.PanelInsetBrush}"
                BorderBrush="{DynamicResource Studio.BorderBrush}"
                BorderThickness="1"
                Padding="9,5"
                VerticalAlignment="Top">
          <TextBlock Text="FEATURE"
                     FontSize="10"
                     FontWeight="SemiBold" />
        </Border>
      </Grid>

      <StackPanel Grid.Row="2"
                  Margin="0,16,0,16"
                  Spacing="12">
        <Border Background="{DynamicResource Studio.PanelInsetBrush}"
                BorderBrush="{DynamicResource Studio.BorderBrush}"
                BorderThickness="1"
                Padding="12">
          <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="96,*">
            <TextBlock Text="Profile"
                       FontWeight="SemiBold" />
            <TextBlock Grid.Column="1"
                       x:Name="ProfileValueTextBlock"
                       TextWrapping="Wrap"
                       Text="Closed sketch profile" />
            <TextBlock Grid.Row="1"
                       Text="Plane"
                       Margin="0,10,0,0"
                       FontWeight="SemiBold" />
            <TextBlock Grid.Row="1"
                       Grid.Column="1"
                       x:Name="PlaneValueTextBlock"
                       Margin="0,10,0,0"
                       Text="Sketch plane" />
            <TextBlock Grid.Row="2"
                       Text="Recipe"
                       Margin="0,10,0,0"
                       FontWeight="SemiBold" />
            <TextBlock Grid.Row="2"
                       Grid.Column="1"
                       Margin="0,10,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Studio.TextMutedBrush}"
                       Text="sweep(profile, distance, twist)" />
          </Grid>
        </Border>

        <Border Background="{DynamicResource Studio.PanelInsetBrush}"
                BorderBrush="{DynamicResource Studio.BorderBrush}"
                BorderThickness="1"
                Padding="12">
          <Grid ColumnDefinitions="*,88" RowDefinitions="Auto,Auto,Auto,Auto">
            <TextBlock Text="Distance"
                       FontWeight="SemiBold" />
            <NumericUpDown x:Name="DistanceInput"
                           Grid.Row="1"
                           Width="160"
                           HorizontalAlignment="Left"
                           Margin="0,8,0,0"
                           Increment="1"
                           Minimum="0.1"
                           Maximum="10000"
                           FormatString="0.###" />
            <Border Grid.Column="1"
                    Grid.Row="1"
                    Margin="8,8,0,0"
                    Background="{DynamicResource Studio.PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource Studio.BorderBrush}"
                    BorderThickness="1"
                    Padding="10,6"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top">
              <TextBlock HorizontalAlignment="Center"
                         Text="mm" />
            </Border>

            <TextBlock Grid.Row="2"
                       Text="Twist"
                       Margin="0,12,0,0"
                       FontWeight="SemiBold" />
            <NumericUpDown x:Name="TwistInput"
                           Grid.Row="3"
                           Width="160"
                           HorizontalAlignment="Left"
                           Margin="0,8,0,0"
                           Increment="5"
                           Minimum="-360"
                           Maximum="360"
                           FormatString="0.###" />
            <Border Grid.Column="1"
                    Grid.Row="3"
                    Margin="8,8,0,0"
                    Background="{DynamicResource Studio.PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource Studio.BorderBrush}"
                    BorderThickness="1"
                    Padding="10,6"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top">
              <TextBlock HorizontalAlignment="Center"
                         Text="deg" />
            </Border>
          </Grid>
        </Border>
      </StackPanel>

      <StackPanel Grid.Row="3"
                  Orientation="Horizontal"
                  HorizontalAlignment="Right"
                  Spacing="8">
        <Button Content="Cancel"
                Classes="FeatureDialogButtonSecondary"
                Click="OnCancelClick" />
        <Button Content="Apply"
                Classes="FeatureDialogButtonPrimary"
                Click="OnConfirmClick" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
